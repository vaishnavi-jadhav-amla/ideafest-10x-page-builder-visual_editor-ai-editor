//----------------------
// <auto-generated>
//     Generated using the NSwag toolchain v13.18.2.0 (NJsonSchema v10.8.0.0 (Newtonsoft.Json v13.0.0.0)) (http://NSwag.org)
// </auto-generated>
//----------------------
import { customFetch } from "../../common/customFetch";
import { getHeaders } from "./base";
import { BStoresRequestRegistrationModel, TrueFalseResponse } from "../../types/multifront-types";

const baseUrl = process.env.API_URL;
/* tslint:disable */
/* eslint-disable */
// ReSharper disable InconsistentNaming

/**
 * Submit BStore registration details.
 * @param registrationData The registration data to submit.
 * @return Success. The request was successfully executed, and the response body contains the registration result.
 */
export async function User_submitBStoreRequest(registrationData: BStoresRequestRegistrationModel): Promise<TrueFalseResponse> {
  let options_: RequestInit = {
    method: "POST",
    headers: {
      ...(await getHeaders("POST", String(baseUrl))),
      "Content-Type": "application/json",
    },
    body: JSON.stringify(registrationData),
    next: { revalidate: 0 },
  };

  let url_ = baseUrl + "BStoresWebStore/BStoresRequestRegistration";
  url_ = url_.replace(/[?&]$/, "");
  return customFetch(url_, options_, "User_submitBStoreRequest").then((_response: Response) => {
    return processBStoreRegistrationResponse(_response);
  });
}

function processBStoreRegistrationResponse(response: Response): Promise<TrueFalseResponse> {
  const status = response.status;
  let _headers: any = {};
  if (response.headers && response.headers.forEach) {
    response.headers.forEach((v: any, k: any) => (_headers[k] = v));
  }
  if (status === 200 || status === 201) {
    return response.text().then((_responseText) => {
      let result: any = null;
      result = _responseText === "" ? null : (JSON.parse(_responseText) as boolean);
      return result;
    });
  } else {
    return response.text().then((_responseText) => {
      return throwException("An error occurred during registration.", status, _responseText, _headers);
    });
  }
}

function throwException(message: string, status: number, response: string, headers: { [key: string]: any }, result?: any): any {
  try {
    if (result !== null && result !== undefined) throw result;
    else throw new Error(message);
  } catch (ex) {
    const parsedRes = response === "" ? null : (JSON.parse(response) as any);
    return parsedRes;
  }
}
