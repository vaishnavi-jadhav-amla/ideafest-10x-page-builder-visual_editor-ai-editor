//  TODO: Payment - need to create separate baseclient for payment manager
// export function getHeaders(requestType: string, baseUrl = ""): Promise<HeadersInit> {
//     return getApiHeaders(requestType);
//   }
  